#include<bits/stdc++.h>   // Includes all standard C++ libraries
using namespace std;      

int main() {
    for(int i = 1; i <= 10; i = i + 1) { // Loop runs from 1 to 10 (10 times)
        cout << "\nHello";               // Prints "Hello" on a new line each time
    }
    return 0;   // Exit program successfully
}
